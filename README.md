# Practise project 

Learing Python and Git

## Git Commands
```
git confif --global user.name
git confif --global user.name "user.name"
git confif --global user.email "xmamatqosimov@gmail.com"

git init 
git status
git add .
git commit -m "git commit name"


```